name: 3105 - Kyojinx Sanity

on: [push, workflow_dispatch]

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Verify workflow execution
        run: echo "Kyojinx workflow recognized"
